import React from 'react'

const About = () => {
  return (
    <div>
      this is about
      <p>

 Adding a Task

Click on the "Add Task" button.

Enter a Title for your task (e.g., "Complete Project Report").

Add a Description (optional) to provide more details (e.g., "Submit the report by 5 PM").

Click Save, and the task will be added to your list.

Managing Tasks

View all your added tasks in a structured list.

Click on a task to mark it as completed or pending.

Edit a task to update its title or description.

Delete tasks that are no longer needed.

Task Completion Tracking

Completed tasks are visually distinct from pending tasks.

You can filter tasks based on their status (All, Completed, Pending).

User-Friendly Design

Clean and minimalistic interface for quick task management.

Responsive design that works on desktop and mobile devices.



      </p>
    </div>
  )
}

export default About
